package com.example.TCC.repository;

import com.example.TCC.model.Salvar;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalvarRepository extends JpaRepository<Salvar, Long> {
}
